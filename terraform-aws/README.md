# Terraform AWS EC2

Provisions a single EC2 instance with Terraform.

## Usage

```bash
terraform init
terraform plan
terraform apply -auto-approve
terraform destroy
⚠️ Never commit *.tfstate or *.tfvars files.
