name: Portfolio CI
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 12 * * 1'  # weekly
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shellcheck
        run: shellcheck **/*.sh
